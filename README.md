# 📰 News Scraper Project (Eitaa MVP)

یک پروژه‌ی آموزشی و پروژه‌محور برای یادگیری پایتون با تمرکز بر:
- معماری تمیز
- اسکریپ اخبار
- پردازش و انتشار محتوا
- آماده‌سازی خروجی برای پیام‌رسان ایتا (Eitaa)

این پروژه به‌صورت **مرحله‌به‌مرحله و Commit-based** توسعه داده می‌شود
و برای استفاده در دوره‌های آموزشی طراحی شده است.

---

## 🎯 اهداف پروژه
- آموزش پایتون به‌صورت پروژه‌محور
- آشنایی با معماری ماژولار
- جمع‌آوری اخبار از منابع مختلف
- پردازش و استانداردسازی داده
- انتشار خروجی در ایتا (MVP)
- آماده‌سازی برای وب و توسعه‌های بعدی

---

## 🧱 معماری کلی پروژه

```text
news_scraper/
│
├── collector/              # جمع‌آوری اخبار از منابع
│   └── news_collector.py
│
├── sources/                # منابع خبری
│   ├── base.py             # قرارداد منبع خبری
│   └── example_source.py   # منبع تستی
│
├── models/                 # مدل‌های داده
│   └── news.py             # مدل استاندارد خبر (dataclass)
│
├── processor/              # پردازش خبر (تمیزسازی، خلاصه‌سازی و...)
│   └── news_processor.py
│
├── publisher/              # انتشار خروجی
│   └── eitaa_client.py     # کلاینت ارسال به ایتا (MVP)
│
├── storage/                # ذخیره‌سازی (آماده برای DB)
│   └── repository.py
│
├── web/                    # خروجی وب (اختیاری)
│   ├── generator.py
│   └── templates/
│       └── index.html
│
├── config/
│   └── settings.py         # تنظیمات پروژه
│
├── data/                   # داده‌های موقت / تست
│
├── main.py                 # نقطه شروع برنامه
├── requirements.txt
├── README.md
└── .gitignore



------------------------توضیح ساختار برنامه تا این لحظه بصور ت فایل به فایل--------------------------------------
### 📄 main.py — نقطه شروع اجرای برنامه

فایل `main.py` قلب اصلی برنامه است و مسئول **اجرای کل جریان (Flow)** پروژه می‌باشد.  
هر زمان که برنامه اجرا می‌شود، این فایل به عنوان نقطه ورود (Entry Point) عمل می‌کند.

به زبان ساده:
> اگر این پروژه یک کارخانه باشد، `main.py` دکمه «روشن» آن است.

---

#### 🎯 مسئولیت‌های اصلی `main.py`

این فایل ۴ وظیفه‌ی مهم را به ترتیب انجام می‌دهد:

1. **جمع‌آوری اخبار (Collect)**
2. **پردازش و پاک‌سازی اخبار (Process)**
3. **ذخیره خروجی به صورت JSON**
4. **آماده‌سازی خروجی برای استفاده در برنامک ایتا**

---

#### 🧭 جریان اجرای برنامه (Execution Flow)

جریان کلی اجرای برنامه به صورت زیر است:

TasnimNewsSource
↓
NewsCollector
↓
NewsProcessor
↓
news.json

yaml

---

#### 🧩 توضیح بخش‌های مهم کد

##### 1️⃣ آماده‌سازی پوشه داده‌ها

```python
DATA_DIR = Path("data")
DATA_DIR.mkdir(exist_ok=True)
پوشه data/ برای ذخیره خروجی نهایی اخبار استفاده می‌شود
اگر پوشه وجود نداشته باشد، به‌صورت خودکار ساخته می‌شود
این کار باعث می‌شود برنامه در اولین اجرا هم بدون خطا کار کند
2️⃣ تابع news_to_dict
python
def news_to_dict(news: News) -> dict:
این تابع وظیفه دارد:

مدل News (که یک dataclass است)
را به یک دیکشنری ساده تبدیل کند
تا بتوان آن را داخل فایل JSON ذخیره کرد
این کار باعث می‌شود:

مدل‌های پایتونی از لایه ذخیره‌سازی جدا بمانند
خروجی JSON کاملاً تمیز و استاندارد باشد
3️⃣ مرحله Collect (جمع‌آوری اخبار)
python
sources = [TasnimNewsSource()]
collector = NewsCollector(sources)
news_list = collector.collect()
در این مرحله:

منابع خبری (Sources) تعریف می‌شوند
NewsCollector هر منبع را صدا می‌زند
خروجی این مرحله لیستی از آبجکت‌های News است
✅ طراحی به‌گونه‌ای است که بتوان به‌راحتی منابع جدید اضافه کرد.

4️⃣ مرحله Process (پردازش اخبار)
python
processor = NewsProcessor()
processed_news = processor.process(news_list)
در این مرحله:

اخبار خام پردازش می‌شوند
حذف داده‌های ناقص
خلاصه‌سازی
یکسان‌سازی فرمت داده‌ها
این مرحله باعث می‌شود خروجی نهایی آماده نمایش در برنامک ایتا باشد.

5️⃣ ذخیره خروجی در فایل JSON
python
output_file = DATA_DIR / "news.json"
خروجی نهایی به صورت news.json ذخیره می‌شود
این فایل مستقیماً توسط:
Web App
Mini App ایتا استفاده می‌شود
✅ این طراحی باعث جداسازی کامل Backend از Frontend شده است.

6️⃣ نقطه شروع برنامه
python
if __name__ == "__main__":
    main()
این بخش مشخص می‌کند که:

فقط زمانی که فایل مستقیماً اجرا شود
تابع main() اجرا گردد
این الگو استاندارد اجرای برنامه‌های پایتون است.

✅ چرا این طراحی آموزشی و حرفه‌ای است؟
✅ تفکیک مسئولیت‌ها (Single Responsibility)
✅ خوانا و قابل فهم برای مبتدی‌ها
✅ قابل توسعه برای پروژه‌های بزرگ‌تر
✅ مناسب برای آموزش معماری نرم‌افزار
✅ کاملاً MVP محور
yaml

---

## ✅ نتیجه آموزشی این بخش

بعد از خواندن این قسمت از README، هنرجو باید بفهمد:
- برنامه از کجا شروع می‌شود
- ترتیب اجرای اجزای سیستم چگونه است
- نقش هر ماژول در معماری چیست

---



### 📄 models/news.py — مدل استاندارد خبر (News Model)

فایل `news.py` مسئول تعریف **ساختار استاندارد یک خبر** در کل پروژه است.  
تمام بخش‌های برنامه (جمع‌آوری، پردازش، ذخیره‌سازی و نمایش) از این مدل استفاده می‌کنند.

به زبان ساده:
> `News` زبان مشترک تمام اجزای این پروژه است.

---

#### 🎯 هدف اصلی این فایل

- تعریف یک مدل واحد برای خبر
- جلوگیری از پراکندگی فرمت داده‌ها
- ساده‌سازی ارتباط بین ماژول‌ها
- آماده‌سازی داده‌ها برای خروجی JSON و Web App

---

#### 🧩 ساختار کلاس `News`

```python
@dataclass
class News:
    title: str
    url: str
    source: str
    published_at: Optional[datetime] = None
    summary: Optional[str] = None
    image_url: Optional[str] = None
این کلاس با استفاده از @dataclass ساخته شده است که باعث می‌شود:

کد خواناتر شود
نیازی به نوشتن __init__ نباشد
مدیریت داده‌ها ساده‌تر شود
📌 توضیح فیلدهای مدل خبر
فیلد	نوع	توضیح
title	str	عنوان خبر
url	str	لینک اصلی خبر
source	str	نام منبع خبری (مثلاً تسنیم)
published_at	datetime | None	زمان انتشار خبر (در صورت وجود)
summary	str | None	خلاصه خبر
image_url	str | None	لینک تصویر خبر
✅ استفاده از Optional باعث می‌شود برنامه در مواجهه با داده‌های ناقص دچار خطا نشود.

🔄 این مدل کجا استفاده می‌شود؟
مدل News در بخش‌های زیر استفاده می‌شود:

sources/* → ساخت خبر خام
collector/news_collector.py → جمع‌آوری خبرها
processor/news_processor.py → پردازش و تمیزکاری
publisher/* → آماده‌سازی برای انتشار
main.py → تبدیل به JSON نهایی
✅ مزایای استفاده از مدل واحد (Data Model)
✅ جلوگیری از تکرار کد
✅ افزایش خوانایی پروژه
✅ تسهیل تست و دیباگ
✅ مناسب برای آموزش معماری لایه‌ای
✅ آماده برای توسعه‌های آینده (API، دیتابیس، کش و …)
🎓 نکته آموزشی مهم
در پروژه‌های واقعی:

همیشه قبل از نوشتن منطق برنامه، مدل داده را طراحی می‌کنیم.

این پروژه دقیقاً همین اصل حرفه‌ای را رعایت کرده است.

yaml

---

## ✅ نتیجه آموزشی این فایل

بعد از خواندن این بخش، هنرجو باید بداند:
- خبر در این پروژه چه ساختاری دارد
- چرا از dataclass استفاده شده
- چرا بعضی فیلدها اختیاری هستند
- چگونه یک مدل، ستون فقرات پروژه را می‌سازد

---
### 📁 sources/ — منابع خبری (News Sources)

پوشه `sources/` شامل کدهای مربوط به **منابع مختلف خبری** است.  
هر منبع خبری مسئول دریافت اخبار از یک وب‌سایت یا سرویس مشخص می‌باشد.

در نسخه فعلی پروژه (MVP آموزشی)، **فقط منبع خبرگزاری تسنیم** استفاده می‌شود تا آموزش ساده و متمرکز باقی بماند.

---

#### 📄 base.py — الگوی استاندارد منابع خبری

فایل `base.py` یک **کلاس پایه (Abstract Base Class)** برای تمام منابع خبری تعریف می‌کند.

```python
class NewsSource(ABC):
    def fetch(self) -> List[News]:
        pass
✅ هدف این فایل:

تعریف یک قرارداد مشترک برای همه منابع خبری
اطمینان از اینکه هر منبع متد fetch() را پیاده‌سازی می‌کند
ساده‌سازی اضافه یا حذف منابع خبری در آینده
به زبان ساده:

هر منبع خبری باید بگوید «چطور خبرها را می‌آورد».

📄 tasnim_source.py — منبع خبرگزاری تسنیم
این فایل مسئول دریافت اخبار از RSS خبرگزاری تسنیم است.

ویژگی‌های اصلی:

استفاده از RSS رسمی تسنیم
دریافت آخرین اخبار (۵ خبر اول)
استخراج عنوان، لینک، خلاصه و تصویر
تبدیل هر خبر به مدل استاندارد News
python
class TasnimNewsSource(NewsSource):
    def fetch(self) -> List[News]:
        ...
✅ چرا RSS؟

ساده
پایدار
مناسب آموزش
بدون نیاز به اسکریپ وب (Scraping)
🖼 استخراج تصویر خبر
در این منبع:

ابتدا از media:content تصویر اصلی استخراج می‌شود
در صورت نبودن، از media:thumbnail استفاده می‌شود
اگر تصویری وجود نداشته باشد، مقدار None برگردانده می‌شود
این طراحی باعث می‌شود:

برنامه در برابر داده‌های ناقص مقاوم باشد
UI بتواند بدون خطا خبرها را نمایش دهد
🧠 نکته آموزشی مهم
در این پروژه:

فقط یک منبع خبری فعال است
سایر منابع (مثال‌ها) برای ساده‌سازی آموزش حذف یا غیرفعال می‌شوند
هدف این تصمیم:

تمرکز هنرجو روی «جریان کلی برنامه»، نه پیچیدگی‌های متعدد

yaml

---

## ✅ نتیجه آموزشی این بخش

بعد از خواندن این قسمت از README، هنرجو می‌فهمد:
- منبع خبری چیست
- چرا یک کلاس پایه داریم
- RSS چیست و چرا انتخاب شده
- تسنیم چطور به مدل `News` تبدیل می‌شود

---
### 📁 collector/ — جمع‌آوری اخبار

پوشه `collector/` مسئول **دریافت اخبار از منابع مختلف خبری** است.  
این بخش هیچ اطلاعی از جزئیات هر منبع ندارد و فقط خروجی نهایی را جمع‌آوری می‌کند.

---

#### 📄 news_collector.py — جمع‌کننده اخبار

فایل `news_collector.py` وظیفه دارد:
- منابع خبری مختلف را دریافت کند
- متد `fetch()` هر منبع را اجرا کند
- خروجی همه منابع را در یک لیست واحد جمع‌آوری کند

```python
class NewsCollector:
    def collect(self) -> List[News]:
        ...
✅ ویژگی‌های مهم این طراحی
منابع خبری به صورت Dependency Injection به کلاس داده می‌شوند
کلاس NewsCollector به نوع منبع وابسته نیست
می‌توان منابع خبری را بدون تغییر این فایل اضافه یا حذف کرد
خطای یک منبع باعث توقف کل برنامه نمی‌شود
🧠 نکته آموزشی
در این پروژه:

NewsCollector فقط «جمع می‌کند»،

نه تحلیل می‌کند، نه پردازش، نه انتشار.

این تفکیک مسئولیت باعث می‌شود:

کد ساده‌تر باشد
آموزش قابل فهم‌تر شود
توسعه در آینده راحت‌تر انجام شود
yaml

---

## ✅ نتیجه آموزشی این فایل

بعد از این بخش، هنرجو باید بفهمد:
- جمع‌آوری خبر یعنی چه
- چرا منبع مستقیماً در main.py صدا زده نمی‌شود
- مزیت جداسازی Collect از Process چیست

---
### 📁 processor/ — پردازش و آماده‌سازی اخبار

پوشه `processor/` مسئول **تمیز کردن و آماده‌سازی اخبار خام** قبل از ذخیره یا نمایش است.  
در این بخش هیچ خبری جمع‌آوری یا منتشر نمی‌شود؛ فقط داده‌ها اصلاح می‌شوند.

---

#### 📄 news_processor.py — پردازش اخبار

فایل `news_processor.py` وظیفه دارد:
- خلاصه خبرها را تمیز کند
- HTML اضافی را حذف کند
- طول خلاصه را محدود کند
- در صورت نبود تصویر، از محتوای خبر تصویر استخراج کند

```python
class NewsProcessor:
    def process(self, news_list: List[News]) -> List[News]:
        ...
✅ کارهایی که در این مرحله انجام می‌شود
حذف تگ‌های HTML از خلاصه خبر
یکسان‌سازی فاصله‌ها و متن
کوتاه‌سازی خلاصه‌ها برای نمایش در UI
استخراج تصویر به‌صورت جایگزین (Fallback)
🧠 نکته آموزشی
در این پروژه:

پردازش خبر جدا از جمع‌آوری خبر انجام می‌شود.

این کار باعث می‌شود:

منبع خبری ساده بماند
خروجی نهایی تمیز و قابل نمایش باشد
تغییرات UI یا خلاصه‌سازی به منابع وابسته نباشد
yaml

---

## ✅ نتیجه آموزشی این فایل

بعد از این بخش، هنرجو باید بداند:
- چرا خلاصه‌ها نیاز به پردازش دارند
- تفاوت «داده خام» و «داده قابل نمایش» چیست
- چرا تصویر به‌صورت جایگزین استخراج می‌شود



----------------
### 📁 storage/ — لایه ذخیره‌سازی داده‌ها

پوشه `storage/` برای مدیریت **ذخیره‌سازی داده‌ها** در پروژه در نظر گرفته شده است.  
در نسخه فعلی (MVP آموزشی)، ذخیره‌سازی به صورت مستقیم در فایل JSON انجام می‌شود و این لایه هنوز پیاده‌سازی نشده است.

---

#### 📄 repository.py — محل توسعه ذخیره‌سازی در آینده

فایل `repository.py` در حال حاضر **خالی است** و عمداً کدی در آن نوشته نشده است.

هدف از وجود این فایل:
- آماده‌سازی ساختار پروژه برای توسعه‌های بعدی
- آموزش مفهوم «لایه ذخیره‌سازی» به صورت ساده
- جلوگیری از شلوغ شدن منطق اصلی برنامه در آینده

---

#### 🧠 نکته آموزشی مهم

در پروژه‌های واقعی:
> منطق ذخیره‌سازی (فایل، دیتابیس، کش و …)  
> نباید داخل `main.py` یا منطق اصلی برنامه نوشته شود.

این فایل نشان می‌دهد که:
- پروژه از ابتدا با نگاه معماری طراحی شده
- اما در MVP فقط چیزهای ضروری پیاده‌سازی شده‌اند

---


### 📁 publisher/ — آماده‌سازی خروجی برای انتشار

پوشه `publisher/` مسئول **آماده‌سازی اخبار برای انتشار در پلتفرم ایتا** است.  
در این لایه، داده‌ها به فرمتی تبدیل می‌شوند که برای Bot یا Mini App ایتا قابل استفاده باشند.

---

#### 📄 eitaa_publisher.py — فرمت‌دهی خبر برای ایتا

فایل `eitaa_publisher.py` وظیفه دارد:
- یک خبر (`News`) را دریافت کند
- آن را به ساختار مناسب برای ایتا تبدیل کند
- بدون انجام ارسال واقعی (فقط آماده‌سازی داده)

```python
class EitaaPublisher:
    def format_message(self, news: News) -> dict:
        ...
✅ خروجی این کلاس:

دیکشنری ساده
سازگار با Mini App
مناسب استفاده در UI یا Bot
🧠 نکته آموزشی
در این پروژه:

«فرمت‌دهی» از «ارسال» جدا شده است.

این کار باعث می‌شود:

تست ساده‌تر شود
وابستگی به API ایتا کاهش یابد
پروژه برای آموزش شفاف‌تر باشد
📄 eitaa_client.py — اتصال واقعی به ایتا (در آینده)
فایل eitaa_client.py در نسخه فعلی پروژه خالی است و هنوز پیاده‌سازی نشده.

هدف از وجود این فایل:

آماده‌سازی پروژه برای اتصال واقعی به Bot ایتا
تفکیک ارتباط شبکه از منطق برنامه
جلوگیری از پیچیدگی زودهنگام در آموزش
در نسخه‌های بعدی، این فایل می‌تواند شامل:

ارسال پیام به Bot ایتا
مدیریت Token
فراخوانی API رسمی ایتا
yaml

---

## ✅ نتیجه آموزشی این بخش

بعد از این قسمت، هنرجو باید بفهمد:
- خروجی پروژه چطور برای ایتا آماده می‌شود
- تفاوت بین «فرمت پیام» و «ارسال پیام» چیست
- چرا بعضی فایل‌ها عمداً خالی هستند

---


### 📁 web/ — نمایش اخبار (Web UI)

پوشه `web/` مسئول **نمایش خروجی برنامه به صورت بصری** است.  
در این بخش، فایل `news.json` که توسط برنامه تولید شده، در قالب یک صفحه وب نمایش داده می‌شود.

هدف این بخش:
- مشاهده خروجی پروژه به‌صورت واقعی
- تکمیل MVP
- استفاده در برنامک ایتا (Mini App)

---

#### 📄 generator.py — تولید صفحات وب (در آینده)

فایل `generator.py` در نسخه فعلی پروژه **خالی است** و هنوز استفاده نمی‌شود.

هدف از وجود این فایل:
- تولید خودکار صفحات HTML از داده‌ها
- جداسازی منطق تولید UI از `main.py`
- آماده‌سازی پروژه برای توسعه‌های بعدی

در نسخه‌های آینده می‌تواند شامل:
- خواندن `news.json`
- ساخت HTML به صورت خودکار
- آماده‌سازی خروجی برای Mini App ایتا

---

#### 📄 index.html — صفحه نمایش اخبار (MVP)

فایل `index.html` یک **صفحه وب ساده و مدرن** برای نمایش اخبار امروز است.

ویژگی‌های اصلی این صفحه:
- کاملاً فارسی و راست‌به‌چپ (RTL)
- طراحی Mobile-first
- استفاده از فونت فارسی استاندارد
- بدون فریم‌ورک (مناسب آموزش)

---

#### 📰 نحوه نمایش اخبار

در این صفحه:
- فایل `data/news.json` به‌صورت مستقیم با `fetch` خوانده می‌شود
- برای هر خبر، یک کارت (Card) ساخته می‌شود
- اطلاعات نمایش داده‌شده:
  - عنوان خبر
  - خلاصه
  - تصویر (در صورت وجود)
  - منبع
  - لینک خبر

---

#### 🎞 اسلایدر بالای صفحه

در ابتدای صفحه:
- یک اسلایدر ساده سه‌تایی قرار دارد
- برای معرفی دوره آموزشی استفاده شده
- تصاویر از پوشه `img/` بارگذاری می‌شوند

این بخش کاملاً مستقل از داده‌های خبری است و صرفاً جنبه نمایشی دارد.

---

#### 🧠 نکته آموزشی مهم

در این پروژه:
> فرانت‌اند عمداً ساده و بدون فریم‌ورک پیاده‌سازی شده است.

هدف:
- تمرکز آموزش روی پایتون
- قابل فهم بودن برای جلسه اول
- جلوگیری از پیچیدگی‌های غیرضروری

---

✅ نتیجه آموزشی این بخش
بعد از این قسمت، هنرجو باید بفهمد:

خروجی برنامه کجا و چگونه نمایش داده می‌شود
ارتباط news.json با UI چیست
چرا بعضی فایل‌ها عمداً خالی هستند
MVP یعنی چه
